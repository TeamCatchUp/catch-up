/**
 * 임베딩 표의 열 정의.
 *
 * Figma 행(`17169:75976` 진행중 · `17169:75993` 실패)은 셋 다 같은 식이다.
 * `716 = 12 | 대상(fill) | 16 | 150 | 16 | 150 | 16 | 32 | 12`
 * 즉 padding 12, gap 16, 대상만 남은 폭을 먹고 나머지 셋은 고정이다.
 * 실패 행에서 대상이 312→314로 늘어난 건 액션이 32→30이라 fill이 흡수한 것이다.
 *
 * `<table>`의 열 알고리즘으로는 이 모델이 안 나온다. `table-fixed`는 모든 열에
 * 폭을 줘야 하고, `table-auto`는 내용 폭을 따라가서 **진행중 표와 히스토리 표의
 * 열이 서로 어긋난다** — 진행중은 시각 칸에 선 18뿐이고 히스토리는 날짜 146이다.
 * 두 표는 같은 패널에 위아래로 쌓이므로 x가 같아야 한다.
 *
 * 그래서 `<table>` 엘리먼트는 두고 행만 grid로 깐다. 템플릿 문자열이 하나라
 * 두 표가 자동으로 정렬된다. display를 덮으면 브라우저가 표 시맨틱을 버리므로
 * `role`을 명시해서 되살린다.
 *
 * 150·32는 Tailwind 스케일에 없는 Figma 실측값이라 임의값으로 둔다.
 * 액션 32는 아이콘 버튼 30을 담는 칸이고, 버튼은 오른쪽 padding 선에 붙는다.
 */
export const EMBEDDING_ROW_GRID = 'grid grid-cols-[minmax(0,1fr)_150px_150px_32px] items-center gap-4 px-3';

/**
 * 임베딩 대상 목록의 열 정의.
 * Figma `17169:75787` — `716 = 12 | 대상(fill) | 16 | 180 | 12`.
 *
 * 범위는 Figma에서 hug지만 `auto`로 두면 안 된다. 행마다 grid가 따로라
 * `auto` 트랙이 그 행의 내용으로 정해지고, 헤더("데이터 범위" 59)와
 * 본문(날짜 172)이 서로 다른 자리에 놓인다. hug의 결과값 180을 고정한다.
 */
export const RESOURCE_ROW_GRID = 'grid grid-cols-[minmax(0,1fr)_180px] items-center gap-4 px-3';

/**
 * 1단계 "템플릿 예시" 패널에 뜨는 문서 양식 6종.
 * 원문은 Confluence 163282945 "VOC 문서 템플릿 양식"이고 키는 WIKI_DOC_KIND_PRESETS의 id다.
 * 표가 있어 렌더 시 remarkGfm이 필요하다.
 */

const FEATURE_REQUEST = `# 기능 요청 문서

> 고객이 원하는 기능과 그 이유, 사용 상황을 하나의 문서로 모읍니다. 제목은 기능 명칭이 아니라 원하는 결과 중심으로 생성됩니다. 예: "상담 데이터를 엑셀로 내려받고 싶어요"

**상태** : \`수집됨\` \`검토중\` \`확정\` \`반영됨\` \`보류\`
**기능 태그** : _폐쇄 어휘에서 부여 — 예: 데이터 반출, 알림, 검색_
**담당자** : _이 문서의 승인 담당_
**최초 접수일** : YYYY-MM-DD | **최근 접수일** : _같은 요구가 마지막으로 들어온 날_

---

## 한 줄 요약

_누가, 무엇을 하고 싶어 하는지 한 문장. 이 문장만 읽어도 요구를 이해할 수 있도록 생성됩니다._

## 원하는 결과

_고객이 얻고자 하는 최종 결과. 구현 방식("버튼을 추가해달라")이 아니라 결과("데이터를 파일로 받아 분석에 쓰고 싶다") 중심으로 정리됩니다._

## 요청 배경

_이 요청이 나온 이유. 고객의 현재 업무 방식과 그 과정에서 발생하는 반복 작업이 담깁니다. 고객이 말한 사실만 기재됩니다._

## 대표 사용 상황

| 상황 | 누가 | 빈도 |
| --- | --- | --- |
| _어떤 업무 장면에서 필요한가_ | _역할 — 예: CS 리더, AM_ | _매일 / 매주 / 월말_ |

## 현재 지원 상태

\`지원\` \`부분 지원\` \`미지원\`

_사실만 표시되며, "곧 지원 예정" 같은 일정 표현은 들어가지 않습니다._

## 현재 가능한 방법

_승인된 우회 방법이 있으면 단계로 정리됩니다. 없으면 "없음"으로 표기되며, 빈칸으로 남지 않습니다._

## 근거 VOC

| 접수일 | 고객사 | 원문 발췌 | 원본 |
| --- | --- | --- | --- |
| _자동 연결_ | _자동 연결_ | _검토 큐에서 승인된 원문만 이 표에 들어옵니다_ | _링크_ |

> **이 문서의 생성 원칙**
> 요청 빈도와 요청 고객사 수는 본문에 고정 기재되지 않습니다 — 근거 VOC 표가 자동 집계를 담당합니다. 고객 표현은 의미가 유지되는 범위에서 일반화되며, 수식어와 평가 표현은 들어가지 않습니다. 표현이 비슷해도 원하는 최종 결과가 다르면 별도 문서로 분리됩니다.`;

const PAIN_POINT = `# 고객 불편사항 문서

> 고객이 어떤 상황에서 불편을 겪는지 재현 가능한 형태로 모읍니다. 제목은 문제 상황 중심으로 생성됩니다. 예: "상담 검색에서 원하는 기록을 찾기 어려워요"

**상태** : \`확인됨\` \`대응 논의중\` \`해소됨\` \`보류\`
**기능 태그** : _문제가 발생하는 기능 영역_
**담당자** : _이 문서의 승인 담당_
**최초 보고일** : YYYY-MM-DD

---

## 한 줄 요약

_어떤 상황에서 어떤 문제가 생기는지 한 문장._

## 발생하는 문제

_의도대로 동작하지 않거나 과도한 시간과 반복 작업을 유발하는 현상. 관찰된 사실만 기재되며, 원인 추정("서버 문제로 보임")은 들어가지 않습니다._

## 기대하는 결과

_고객이 원래 기대했던 동작이나 결과._

## 발생 조건

_다른 사람이 재현할 수 있도록 조건 중심으로 정리됩니다._

1. _어떤 상태에서 — 예: 동일 고객사 상담 이력이 많은 경우_
2. _어떤 행동을 하면 — 예: 기간 필터 없이 키워드만으로 검색_
3. _어떤 결과가 나오는가_

## 영향

_이 문제로 실제 발생하는 결과. "고객이 크게 불만" 같은 평가가 아니라 "상담 준비 시간이 길어진다"처럼 관찰된 사실이 담깁니다._

## 현재 대응

_문의가 들어왔을 때의 승인된 안내 기준. 없으면 "없음"으로 표기됩니다._

## 근거 VOC

| 접수일 | 고객사 | 원문 발췌 | 원본 |
| --- | --- | --- | --- |
| _자동 연결_ | _자동 연결_ | _승인된 원문만 적재_ | _링크_ |

> **이 문서의 생성 원칙**
> 원인을 단정하지 않습니다 — 판정은 사람의 몫입니다. 증상 표현이 달라도 같은 문제 상황이면 별도 문서가 아니라 기존 문서에 병합 제안됩니다. 영향은 관찰된 사실만 기재됩니다.`;

const FAQ = `# 자주 묻는 질문 문서

> 반복되는 질문과 현재 기준 답변을 모아, 누가 답해도 같은 답이 나오게 합니다. 제목은 고객이 실제로 묻는 형태로 생성됩니다. 예: "상담 데이터를 엑셀로 받을 수 있나요?"

**상태** : \`답변 확정\` \`기준 확인 필요\`
**카테고리** : \`요금\` \`데이터\` \`계정\` \`연동\`
**답변 확정자** : _현재 답변을 승인한 사람_
**최종 확인일** : _이 답이 아직 유효한지 마지막으로 확인된 날_

---

## 이렇게도 물어요

_같은 것을 묻는 다른 표현들이 쉼표로 정리됩니다. 검색과 병합 판단의 근거가 됩니다._
_예: "상담 내역 다운로드 되나요", "대화 기록 추출 가능한가요"_

## 현재 답변

> _고객에게 그대로 복사해 보낼 수 있는 완성된 문장으로 생성됩니다. 내부 용어와 일정 약속은 들어가지 않습니다._

## 가능한 방법

_우회 방법이나 대안이 있으면 고객이 따라 할 수 있는 단계로 정리됩니다._

1. _1단계_
2. _2단계_

## 예외 및 주의사항 (내부용)

_실무자만 알아야 할 맥락 — 예외 조건, 특정 고객사의 별도 계약 조건, 과거 이슈. 이 섹션은 고객 전달용이 아닙니다._

## 관련 질문

_함께 자주 묻는 다른 질문 문서와 연결됩니다._

## 근거 VOC

| 접수일 | 고객사 | 질문 원문 | 원본 |
| --- | --- | --- | --- |
| _자동 연결_ | _자동 연결_ | _승인된 원문만 적재_ | _링크_ |

> **이 문서의 생성 원칙**
> 현재 답변이 문서의 중심입니다 — 누가 답해도 같은 답이 나오게 하는 것이 목적입니다. 고객 전달용(현재 답변, 가능한 방법)과 내부용(예외 및 주의사항)은 섞이지 않습니다. 답변의 근거가 되는 기준이 바뀌면 충돌로 감지되어 검증 배지가 경고 상태로 전환됩니다.`;

const CLIENT_REQUEST = `# 고객사별 요청사항 문서

> 특정 고객사가 요청한 기능과 조건을 고객사 맥락과 함께 모읍니다. 제목은 고객사명과 요청 내용으로 생성됩니다. 예: "A사 — 상담 데이터를 주 단위로 자동 전달받고 싶어요"

**고객사** : _고객사 히스토리 문서와 연결_
**상태** : \`접수\` \`검토중\` \`답변 완료\` \`반영됨\` \`거절\`
**담당자** : _이 고객사의 우리 측 담당_
**요청일** : YYYY-MM-DD | **계약 관련 여부** : \`예\` \`아니오\`

---

## 한 줄 요약

_어느 고객사가 무엇을 어떤 조건으로 요청했는지 한 문장._

## 요청 내용

_고객사가 원하는 결과. 구현 방식이 아니라 결과 중심으로 정리됩니다._

## 요청 배경

_이 고객사가 왜 이 요청을 하는지 — 사용 맥락, 업무 방식, 사내 프로세스._

## 조건과 제약

| 구분 | 내용 | 출처 |
| --- | --- | --- |
| 시점 | _언제까지 필요한가 — 고객이 말한 사실만_ | _상담 원문_ |
| 보안 | _보안, 개인정보 요건_ | _상담 원문_ |
| 형식 | _파일 형식, 전달 방식 등_ | _상담 원문_ |

## 현재 지원 상태

\`지원\` \`부분 지원\` \`미지원\`

## 논의 경과

| 날짜 | 채널 | 내용 | 전달한 답변 |
| --- | --- | --- | --- |
| YYYY-MM-DD | \`채널톡\` \`미팅\` \`메일\` | _오간 내용 사실만_ | _"검토 대상 안내"와 "지원 예정 안내"가 구분되어 기록됩니다_ |

## 관련 문서

_같은 요구의 전사 기능 요청 문서와 연결됩니다. 공통 내용은 전사 문서에, 이 고객사 고유 조건만 이 문서에 남습니다._

> **이 문서의 생성 원칙**
> 매출, 계약 금액 등 CRM 영역은 다루지 않습니다. 약속하지 않은 것이 약속한 것처럼 기록되지 않습니다. 전사 요구 문서와 중복 서술하지 않고, 고객사 고유 조건만 남깁니다.`;

const CLIENT_HISTORY = `# 고객사 히스토리 문서

> 고객사와 오간 문의, 요청, 결정을 시간순으로 한 문서에 모읍니다. 고객사당 1개 문서이며, 개별 요청의 상세는 고객사별 요청사항 문서로 분리됩니다.

**업종 / 규모** : _승인된 사실만_
**도입 시기** : YYYY-MM
**우리 측 담당** : _현재 담당자_ | **고객 측 담당** : _현재 담당자 — 변경 시 이력 표에 보존_

---

## 고객 특징

_업종, 규모, 팀 구성, 제품 사용 방식 등 승인된 사실. 이 영역은 사람의 승인 없이 바뀌지 않습니다._

## 사용 맥락

_이 고객사가 제품을 어떤 업무 흐름에서 쓰는지, 반복되는 문의 주제는 무엇인지._

## 담당자 이력

| 기간 | 우리 측 | 고객 측 |
| --- | --- | --- |
| YYYY-MM ~ 현재 | _이름_ | _이름_ |
| YYYY-MM ~ YYYY-MM | _이전 담당 — 삭제되지 않고 이력으로 보존_ | _이름_ |

## 주요 결정과 합의

| 날짜 | 내용 | 근거 |
| --- | --- | --- |
| YYYY-MM-DD | _결정 또는 합의 내용 — 사실만, 해석 없이_ | _원본 대화 링크_ |

## VOC 타임라인

| 날짜 | 유형 | 내용 | 연결된 문서 |
| --- | --- | --- | --- |
| _자동 적재_ | \`요구\` \`버그\` \`질문\` \`기타\` | _모든 VOC가 유형과 무관하게 쌓입니다 — 버려지는 신호는 없습니다_ | _자동 연결_ |

> **이 문서의 생성 원칙**
> 검증 배지는 문서 단위로 붙습니다. 사람 확인 영역의 변경만 승인 대상이며, 자동 집계 영역은 배지와 무관하게 시스템이 갱신합니다. 사람 확인 영역과 자동 집계 영역은 섞이지 않습니다. 자동 집계 영역의 수치는 시스템이 관리합니다 — 본문에 수기로 고정 기재되지 않습니다. 담당자 변경은 덮어쓰지 않고 이력으로 보존됩니다.`;

const POLICY = `# 정책과 예외사항 문서

> 현재 적용 기준과 예외로 처리되는 경우를 근거와 함께 모읍니다. 이 문서의 존재 이유는 "지금 뭐가 맞는가"에 바로 답하는 것입니다. 제목은 정책 대상 중심으로 생성됩니다. 예: "데이터 보관 기간 기준"

**상태** : \`적용중\` \`개정 논의중\`
**적용 시작일** : YYYY-MM-DD
**결정자** : _이 기준을 확정한 사람 또는 회의체_
**최종 확인일** : _기준이 아직 유효한지 마지막으로 확인된 날_

---

## 한 줄 요약

_현재 기준을 한 문장으로._

## 현재 기준

_지금 적용되는 기준. 조건과 수치가 명확히 기재되며, "대체로", "보통" 같은 모호한 표현은 들어가지 않습니다._

## 적용 범위

| 대상 | 적용 여부 | 비고 |
| --- | --- | --- |
| _플랜 / 고객 유형 / 기능 범위_ | \`적용\` \`미적용\` |  |

## 예외로 처리되는 경우

| 예외 상황 | 처리 기준 | 승인 절차 | 근거 |
| --- | --- | --- | --- |
| _어떤 경우에_ | _어떻게 처리하는가_ | _누구의 승인이 필요한가_ | _결정 기록 링크 — 근거 없는 예외는 기재되지 않습니다_ |

## 결정 배경

_이 기준이 왜 이렇게 정해졌는지. 확인 가능한 경우만 기재되며, 추정은 들어가지 않습니다._

## 예외 및 주의사항 (실무자용)

_자주 혼동되는 지점, 과거 이슈. 예: "삭제 요청 즉시 삭제"와 혼동하는 문의가 반복됨._

## 개정 이력

| 날짜 | 변경 내용 | 버전 |
| --- | --- | --- |
| YYYY-MM-DD | _무엇이 어떻게 바뀌었는지 — 덮어쓰지 않고 새 버전으로_ | v1 |

> **이 문서의 생성 원칙**
> 현재 기준이 문서의 가장 앞에 옵니다 — 과거 기준은 개정 이력이 담당합니다. 근거 없는 예외는 미확정 사항이므로 기재되지 않습니다. 기준이 바뀌면 새 버전으로 보존됩니다 — 과거 기준으로 처리된 건을 추적할 수 있습니다.`;

/** 문서 종류 id → 템플릿 마크다운 */
export const WIKI_DOC_TEMPLATE_SAMPLES: Readonly<Record<string, string>> = {
  feature_request_status: FEATURE_REQUEST,
  complaint_topic_brief: PAIN_POINT,
  faq_answer: FAQ,
  customer_voice_profile: CLIENT_REQUEST,
  customer_history: CLIENT_HISTORY,
  // 서버 카탈로그에 대응 종류가 없다 — 시안에만 있는 카드다
  policy: POLICY,
};

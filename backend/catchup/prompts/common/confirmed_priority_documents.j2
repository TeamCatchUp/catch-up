{% if confirmed_priority_indices %}
<confirmed_priority_documents indices="<< confirmed_priority_indices >>">
  The search agent flagged these documents as critical, AND the reranker independently
  confirmed them as highly relevant. Two independent signals agreeing makes the evidence
  more reliable — treat them as anchor evidence:

  <instruction>
    - Cite them where directly applicable; do not omit them if factually relevant.
    - Use them to ground claims, then enrich with other documents in retrieved documents for context and cross-reference.
    - This is a priority hint, not an exclusivity rule — all documents in retrieved documents remain available; use whichever best supports each claim.
  </instruction>
</confirmed_priority_documents>
{% endif %}
